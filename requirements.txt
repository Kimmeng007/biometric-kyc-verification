# PyTorch: install first from the matching index, e.g.
#   pip install torch==2.14.0 torchaudio --index-url https://download.pytorch.org/whl/cu130   (NVIDIA GPU)
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu             (CPU only)
torch>=2.4
torchaudio>=2.4
speechbrain==1.1.1
insightface==2.0
onnxruntime-gpu==1.30.0   # or `onnxruntime` on CPU-only machines
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.1
scikit-image==0.26.0
matplotlib==3.11.2
opencv-python-headless==5.0.0.93
soundfile==0.14.0
tqdm==4.70.1
gradio==6.28.0        # web demo (app.py)
